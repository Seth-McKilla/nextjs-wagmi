{
  "name": "PredictOnChain",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ <Github>/PredictOnChain",
    "create-local": "graph create --node http://localhost:8020/ <Github>/PredictOnChain",
    "remove-local": "graph remove --node http://localhost:8020/ <Github>/PredictOnChain",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 <Github>/PredictOnChain"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
